{
  "name": "dev-root",
  "private": true,
  "scripts": {
    "lint": "pnpm -r lint",
    "format": "pnpm -r format",
    "test": "pnpm -r test",
    "prepare": "husky install"
  },
  "devDependencies": {
    "biome": "^0.3.3",
    "husky": "^9.1.7",
    "typescript": "^5.0.0"
  }
}
